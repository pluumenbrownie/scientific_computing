To run this project:
1. Install dependencies from the `pyproject.toml` file.
2. Run the main program file with `python src/main.py` to generate the figures seen in the report.
    - Windows users may need to create a `figures` folder in `src`.
3. Generate the plots for DLA seperately by running `python src/diffusion_limited.py`.